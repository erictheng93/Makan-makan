# MakanMakan Security Environment Variables Template
# Copy this file to .env.local for development or configure in production

# ========== JWT Security Configuration ==========
# JWT secret key for token signing and verification
# SECURITY: Must be at least 32 characters long and cryptographically secure
# Generate with: openssl rand -base64 48
# Generate your own with: openssl rand -base64 48
# Example secure key (DO NOT USE THIS EXACT VALUE):
JWT_SECRET=4GIgZlJURMeuoRUmaS09RNKwYCAAW0xLgAteLPzYGH2TaaATEEbHCZ2zHJEQyVEE

# ========== Database Configuration (Legacy PHP) ==========
# Database connection settings for legacy PHP components
# SECURITY: Never commit real passwords to version control
DB_HOST=localhost
DB_NAME=makanmakan
DB_USER=your_db_username
DB_PASSWORD=your_secure_database_password

# ========== Cloudflare Workers Configuration ==========
# Environment setting (development, staging, production)
NODE_ENV=development

# Cloudflare API Token for deployments
# Get from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token

# ========== Cloudflare Images API ==========
# For image processing and optimization
# Get from: Cloudflare Dashboard > Images > API
CLOUDFLARE_IMAGES_KEY=your_cloudflare_images_api_key
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id

# ========== Error Reporting & Monitoring ==========
# Slack webhook for critical error notifications
# Create at: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Sentry DSN for error tracking (optional)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ========== Rate Limiting & Security ==========
# Rate limiting configuration (requests per minute)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# Security headers configuration
SECURITY_HEADERS_ENABLED=true

# ========== Development Settings ==========
# Only for development - set to 'false' or remove in production
DEBUG_MODE=false
VERBOSE_LOGGING=false

# ========== IMPORTANT SECURITY NOTES ==========
# 1. NEVER commit this file with real values to version control
# 2. Use different secrets for each environment (dev/staging/prod)
# 3. Rotate secrets regularly (at least quarterly)
# 4. Use strong, randomly generated passwords and secrets
# 5. Limit database user permissions to minimum required
# 6. Enable SSL/TLS for all database connections in production
# 7. Use Cloudflare WAF and security features
# 8. Monitor access logs and set up alerts

# ========== Production Deployment Notes ==========
# For Cloudflare Workers deployment:
# 1. Set these variables in wrangler.toml [env.production.vars] section
# 2. Use 'wrangler secret put' for sensitive values like JWT_SECRET
# 3. Configure Cloudflare D1, KV, and R2 bindings
# 4. Enable Cloudflare security features (WAF, DDoS protection)
# 5. Set up monitoring and alerting

# Generate secure secrets:
# JWT_SECRET: openssl rand -base64 48
# Database password: openssl rand -base64 32
# API keys: Use provider-generated keys