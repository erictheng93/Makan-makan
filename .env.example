# MakanMakan Environment Variables Example
# Copy this file to .env.local and fill in your actual values

# ========== JWT Security Configuration ==========
# CRITICAL: Generate a secure JWT secret using: openssl rand -hex 32
# Minimum 32 characters required for security
JWT_SECRET=your_generated_jwt_secret_here_must_be_at_least_32_characters_long

# ========== Cloudflare Configuration ==========
# Get these from your Cloudflare dashboard
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here
CLOUDFLARE_IMAGES_KEY=your_cloudflare_images_key_here

# ========== Database Configuration (Legacy PHP only) ==========
DB_HOST=localhost
DB_NAME=makanmakan
DB_USER=root
DB_PASSWORD=your_database_password_here

# ========== External Services ==========
# Optional: Slack webhook for error notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/slack/webhook

# ========== Development Settings ==========
NODE_ENV=development
DEBUG_MODE=true
VERBOSE_LOGGING=true

# ========== Rate Limiting & Security ==========
RATE_LIMIT_REQUESTS=1000
RATE_LIMIT_WINDOW_MS=60000
SECURITY_HEADERS_ENABLED=true

# ========== PRODUCTION DEPLOYMENT NOTES ==========
# For production:
# 1. Use wrangler secret put JWT_SECRET to set JWT secret securely
# 2. Never commit actual secrets to version control
# 3. Use different secrets for staging vs production
# 4. Generate new JWT_SECRET: openssl rand -hex 32